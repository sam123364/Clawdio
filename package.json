{
  "name": "bouncer-ai",
  "version": "1.0.0",
  "description": "Base OpenClaw agent with Telegram integration",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env node_modules/openclaw/dist/cli.js gateway run",
    "start:win": "powershell -Command \"$env:OPENROUTER_API_KEY=(Get-Content .env | Where-Object { $_ -match '^OPENROUTER_API_KEY=' } | ForEach-Object { $_.Split('=', 2)[1].Trim() }); npx openclaw gateway run\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "openclaw": "^2026.5.7"
  }
}
